southern-quiet:
  framework:
    file-system:
      local:
        working-root: ${user.home}/.SQ_FILESYSTEM
    key-value:
      file-system:
        working-root: TMP_KEY_VALUE
        name-separator: _|_
