dependencies {
    api("org.springframework.boot:spring-boot-starter")
    implementation "me.insidezhou.instep:instep-core:${instepVersion}"

    testImplementation "org.springframework.boot:spring-boot-starter-web"
}
