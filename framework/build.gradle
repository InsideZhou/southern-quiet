dependencies {
    api("org.springframework.boot:spring-boot-starter")
    api "me.insidezhou.instep:instep-core:${instepVersion}"

    testImplementation "org.springframework.boot:spring-boot-starter-json"
}
